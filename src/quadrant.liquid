<style>
  .map { position: relative; height: 140px; width: 122px; margin: 0 auto; }
  .map-body { position: absolute; left: 50%; transform: translate(-50%, -50%); }
  .map-earth { position: absolute; left: 50%; top: 81%; transform: translate(-50%, -50%); }
  .map-craft { position: absolute; transform: translate(-50%, -50%); z-index: 1; }
  .trail-dot { position: absolute; width: 1px; height: 1px; border-radius: 9999px; background: black; transform: translate(-50%, -50%); z-index: 2; }
  .circle { border-radius: 9999px; }
  .capsule-wrap {
    filter:
      drop-shadow(.5px 0 0 white) drop-shadow(-0.5px 0 0 white)
      drop-shadow(0 0.5px 0 white) drop-shadow(0 -0.5px 0 white)
      drop-shadow(0.5px 0.5px 0 white) drop-shadow(-0.5px 0.5px 0 white)
      drop-shadow(0.5px -0.5px 0 white) drop-shadow(-0.5px -0.5px 0 white);
  }
  .capsule {
    width: 5px;
    height: 5px;
    background: black;
    clip-path: polygon(0% 0%, 100% 0%, 75% 100%, 25% 100%);
  }
</style>

<div class="layout layout--col layout--center-x gap--xsmall">
  <!-- Map area -->
  <div class="map w--full">
    <!-- Moon -->
    <div class="map-body" style="top: {{ moon_y_pct }}%;">
      <div class="circle bg--black w--1 h--1"></div>
    </div>

    <!-- Trail -->
    {% for point in trail %}
      <div class="trail-dot 1bit:bg--black 2bit:bg--gray-50 4bit:bg--gray-50" style="left: {{ point.x }}%; top: {{ point.y }}%;"></div>
    {% endfor %}
    <div class="trail-dot 1bit:bg--black 2bit:bg--gray-50 4bit:bg--gray-50" style="left: {{ craft_x_pct }}%; top: {{ craft_y_pct }}%;"></div>

    <!-- Flyby Moon (where the moon was at closest approach) -->
    <div style="position: absolute; left: {{ flyby_moon_x_pct }}%; top: {{ flyby_moon_y_pct }}%; transform: translate(-50%, -50%); z-index: 0;">
      <div style="width: 4px; height: 4px; border-radius: 9999px; border: 1px dashed black;"></div>
    </div>

    <!-- Spacecraft -->
    <div class="map-craft" style="left: {{ craft_x_pct }}%; top: {{ craft_y_pct }}%;">
      <div class="capsule-wrap" style="rotate: {{ craft_heading_deg }}deg;"><div class="capsule"></div></div>
    </div>

    <!-- Earth -->
    <div class="map-earth">
      <div class="circle bg--black w--7 h--7"></div>
    </div>
  </div>

  <!-- Stats -->
  <span class="title title--small">Artemis II</span>
  <div class="flex flex--row flex--center-x gap--medium">
    <div class="flex flex--col flex--center-x gap--none">
      <span class="label label--small label--base">Earth</span>
      <span class="value value--small">{{ distance_earth_km }}</span>
    </div>
    <div class="flex flex--col flex--center-x gap--none">
      <span class="label label--small label--base">Moon</span>
      <span class="value value--small">{{ distance_moon_km }}</span>
    </div>
  </div>
</div>
